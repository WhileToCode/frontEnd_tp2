class articles {
    constructor(article, quantity, checked) {
        this.article = article
        this.quantity = quantity
        this.checked = checked
    }

    toString() {
        return `${this.article}`
    }
}
